{# étend le template depuis base.html.twig, qui est donc son parent #}
{% extends "template.html.twig" %}

{# on veut surcharger le title, on utilise le bloc, on récupère le contenu du parent {{ parent() }} et on rajoute notre texte #}
{% block title %}
    {{ parent() }}
    | Admin | Ajouter une filiere
{% endblock %}

{% block menuhaut %}
    
{% endblock %}

{% block centre %}
    <!-- Page Content -->
    <main role="main" class="container">
        <div class="row">
            <div class="col-lg-12 text-center">
                <h1 class="mt-5">Administration du CF2m</h1>
                <p class="lead">Ajouter un utilisateur </p>
            </div>
            <div
                class="col-lg-6 text-left">
                
                {# Affichage du formulaire #}
                <form action="" method="post" name="createFiliere">
                <div class="form-group"><br>
                <label for ="Nom">Nom:</label><br>
                <input name="lenom"  maxlength="45" type="text" class="form-control" placeholder="Ajout le nom" required><br>
                <label for ="Prenom">Prénom:</label>
                <input name="leprenom"  maxlength="180" type="text" class="form-control" placeholder="Ajout le prénom" required><br>
                <label for ="email">e-mail:</label>
				<input name="lemail" maxlength="180" type="email" class="form-control" placeholder="Ajout email" required><br><br>

                <label for="role">Rôle:</label>
				<select name="role" id="role" class ="form-control" required>
					<option value=""> -- Choix du rôle --</option>
					{% for item in roles %}
				<option value={{item.idlerole}}>{{item.lintitule}}</option>
				{% endfor %}


   
</select><br><br>
    <button type="submit" class="btn btn-primary">Envoyer</button>
                {{ pagination|raw }}    
                </form>
                <h3>{{ error }}</h3>
            </div>
        </div>
    </main> 
 

{% endblock %}

{% block bas %}
    {% include "footer.html.twig" %}
{% endblock %}
